# Hyperflow Python Dependencies
# Install with: pip install -r requirements.txt

# =============================================================================
# CORE REQUIREMENTS
# =============================================================================
pyyaml>=6.0

# Google API integration (Gmail, Calendar)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# =============================================================================
# ENTITY EXTRACTION (Phase 1)
# =============================================================================
# Google's LangExtract for structured entity extraction with source grounding
# Requires Ollama running locally for LLM backend
# langextract>=0.1.0  # Uncomment when available on PyPI

# spaCy for fast offline NER preprocessing
spacy>=3.7.0
# After installing, run: python -m spacy download en_core_web_md

# =============================================================================
# CONTENT INGESTION (Phase 2)
# =============================================================================
# PDF to Markdown conversion
# marker-pdf>=0.2.0  # Heavy dependency, uncomment if needed

# Modern HTTP client (for Jina Reader API)
httpx>=0.27.0

# arXiv paper ingestion
arxiv>=2.1.0

# DOI resolution
# habanero>=1.2.0  # For CrossRef DOI lookup

# =============================================================================
# UTILITIES
# =============================================================================
# Rich terminal output
rich>=13.0.0

# Click for CLI interfaces
click>=8.1.0

# Python-frontmatter for YAML parsing in markdown
python-frontmatter>=1.0.0

# =============================================================================
# PUBLISHING (Phase 3)
# Note: Static site generators are Node-based, installed separately
# - Quartz: npx quartz create
# - Jekyll: gem install bundler jekyll
# - Eleventy: npm install @11ty/eleventy
# - Gatsby: npm install gatsby
# =============================================================================
# ghp-import for GitHub Pages deployment (Jekyll)
ghp-import>=2.1.0

# =============================================================================
# OPTIONAL: Local LLM Integration
# =============================================================================
# Ollama Python client for local LLM calls
ollama>=0.3.0

# =============================================================================
# DEVELOPMENT / TESTING
# =============================================================================
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.0.0

# =============================================================================
# NOTES
# =============================================================================
# After installation, run these commands:
#
# 1. Download spaCy model:
#    python -m spacy download en_core_web_md
#
# 2. Install Ollama (for LangExtract):
#    curl -fsSL https://ollama.com/install.sh | sh
#    ollama pull llama3.2
#
# 3. For PDF ingestion (marker has heavy deps):
#    pip install marker-pdf
#
# 4. For static site publishing:
#    - Quartz: npx quartz create
#    - Jekyll: gem install bundler jekyll
#    - Eleventy: npm install -g @11ty/eleventy
